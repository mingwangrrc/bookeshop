<%= form_with(model: author, local: true) do |form| %>
  <!-- Other fields -->
  <div class="field">
    <%= form.label :profile_picture %>
    <%= form.file_field :profile_picture %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>