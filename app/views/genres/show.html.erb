<h1 class="text-3xl font-bold mb-4"><%= @genre.name %></h1>

<h2 class="text-2xl font-bold mt-5">Books in this Genre</h2>
<ul>
  <% @genre.books.each do |book| %>
    <li><%= link_to book.title, book_path(book) %></li>
  <% end %>
</ul>

<%= link_to 'Back to Genres', genres_path %>